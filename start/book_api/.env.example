# PostgreSQL Configuration
POSTGRES_USER=myuser
POSTGRES_PASSWORD=mypassword
POSTGRES_DB=mydb
POSTGRES_PORT=5432

# Application Configuration (localhost for local dev)
DB_HOST=localhost
DB_PORT=5432
PORT=8080
DATABASE_URL=postgresql://myuser:mypassword@localhost:5432/mydb?sslmode=disable

# JWT Secret Key
SECRET_TOKEN=<REPLACE WITH A RANDOM LONG STRING>